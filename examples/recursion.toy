


//const factorial = i => {
//	if (i <= 1) {
//		return i;
//	}

//	print i;

//	return factorial(i - 1);
//};

//WARNING: it chokes on this
const fibonacci = i => {
	if (i <= 2) {
		return i;
	}

	return fibonacci(i - 1) + fibonacci(i - 2);
};



print factorial(12);


//goes forever, until stack overflow
const count = (i) => {
	print i += 1;

	if (i >= 10) {
		return;
	}

	count(i);
};

count(0);