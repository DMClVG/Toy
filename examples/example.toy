print 1 + 2 - 3 * 4 / 5; //0.6
print 5 % 2; //1

//interpolated strings don't work yet.
print "Hello world," + "foobar" + `interpolation ${5 * 6}`;

/*
var dict = [:];

dict["foo"] = "bar";
dict["baz"] = "fiz";

print dict.foo; //bar
print dict.baz; //fiz

dict::ForEach((key, value) => {
	//assume key & value are strings
	print key + " = " + value;
});
*/
