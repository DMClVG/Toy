#ifndef CTOY_GRAMMAR_RULES_H
#define CTOY_GRAMMAR_RULES_H

#include "parser.h"

//grammar rules
void declaration(Parser* parser);
void statement(Parser* parser);
void expression(Parser* parser);

#endif
